{
  "name": "nap-mcp-server",
  "version": "1.0.0",
  "description": "MCP Server for NAP API",
  "main": "dist/server.js",
  "type": "module",
  "scripts": {
    "start": "ts-node src/server.ts",
    "dev": "nodemon --watch src -e ts --exec ts-node src/server.ts",
    "build": "tsc",
    "clean": "rimraf dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "mcp",
    "nap",
    "api"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "@types/axios": "^0.14.4",
    "axios": "^1.8.4",
    "dotenv": "^16.0.3",
    "jsonwebtoken": "^9.0.0",
    "zod": "^3.21.4"
  },
  "devDependencies": {
    "@types/jsonwebtoken": "^9.0.1",
    "@types/node": "^18.15.3",
    "nodemon": "^3.0.0",
    "rimraf": "^5.0.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.0"
  }
}
